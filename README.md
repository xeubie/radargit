radargit is a very basic TUI for git. It is really only meant as an example project for [xitui](https://github.com/xit-vcs/xitui). It only includes an interface for log and status.

To run, install zig 0.16.0 and do `zig build run`.
